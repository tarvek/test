name: Ansible
on: workflow_dispatch
jobs:
  ok:
    runs-on: ubuntu-latest
    container: 
      image: quay.io/ansible/ansible-runner:latest
    steps:
      - name: run action
        uses: ./.github/actions/myactions
      - name: run workflow
        uses: ./.github/workflows/new.yml
     
